#' * `filter_pool_per_sample` :: `numeric(1)`\cr
#'   `filter_pool_per_sample` parameter of the surrogate model filtering algorithm, see the corresponding section.
#'   Initialized to 1. Together with the default of `filter_pool_per_sample`, this is equivalent to random sampling new individuals.
