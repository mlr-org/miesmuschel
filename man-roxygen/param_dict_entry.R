#' @param dict_entry (`character(1)` | `NULL`)\cr
#'   Key of the class inside the [`Dictionary`][mlr3misc::Dictionary] (usually one of
#'   [`dict_mutators`], [`dict_recombinators`], [`dict_selectors`]), where it can
#'   be retrieved using a [short access function][mut]. May be `NULL` if the operator
#'   is not entered in a dictionary.\cr
#'   The `$dict_entry` field will reflect this value.
